@model DLOA.Model.UserInfo
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Creat</title>
    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
</head>
<body>


        @*
              @using (Ajax.BeginForm("Creat","UserInfo",new AjaxOptions {

            OnSuccess="alert('ok');"
        }))
        {
            <div>
                @Html.LabelFor(m => m.UserName)
                @Html.TextBoxFor(m => m.UserName)
            </div>
            <div>
                @Html.LabelFor(m=>m.)
                @Html.TextBoxFor(m => m.UserAge)


            </div>

            <div>
                @Html.LabelFor(m => m.UserGender)
                <input type="radio" name="UserGender" value="true" />男
                <input type="radio" name="UserGender" value="false" />女
            </div>


            <input  type="submit" value="提交" />

        }
    *@
  

</body>
</html>
