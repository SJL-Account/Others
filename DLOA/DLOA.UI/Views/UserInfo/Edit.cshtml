@model DLOA.Model.UserInfo
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Edit</title>

    <link href="~/Content/themes/default/easyui.css" rel="stylesheet" />
    <link href="~/Content/themes/icon.css" rel="stylesheet" />
    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
    <script src="~/Scripts/jquery.easyui.min.js"></script>
    <script src="~/Scripts/easyui-lang-zh_CN.js"></script>
    <script type="text/javascript"></script>
    <script src="~/Scripts/jquery-1.7.1.min.js"></script>
    <script src="~/Scripts/jquery.unobtrusive-ajax.min.js"></script>
    <link href="~/Content/themes/bootstrap/bootstrap.min.css" rel="stylesheet" />
    <script>

        function EditSubmit()
        {
            $('form').submit();
        }

        function EditAfter(msg)
        {
            window.parent.EditAfter(msg);
        }
    </script>
</head>
<body>
@using (Ajax.BeginForm("Edit", "UserInfo", new AjaxOptions
{
    OnSuccess = "EditAfter"
}))
{
    
    
    <label>用户名</label>
    @Html.TextBoxFor(m => m.UserName,new {@class="form-control"})
    <label>密码</label>


    <input type="password"  name="UserPwd"  value="      " class="form-control" />
    <input type="hidden"  name="UserPwdHidden" value="@Model.UserPwd" />
    
    <label>备注</label>
    @Html.TextBoxFor(m => m.Remark, new { @class = "form-control" })
    <input type="hidden" name="UserId" value="@Model.UserId"  />
    
    
    @*<table>
        <tr>
            <td><label>用户名</label></td>
            <td>@Html.TextBoxFor(m => m.UserName,new {@class="form-control"})</td>
        </tr>
        <tr>
            <td><label>密码</label></td>
            <td><input type="password"  name="UserPwd"  value="      " class="form-control" />
                <input type="hidden"  name="UserPwdHidden" value="@Model.UserPwd" />
            </td>
            

        </tr>
        <tr>
            <td><label>备注</label>)</td>
            <td>@Html.TextBoxFor(m => m.Remark, new { @class = "form-control" })</td>

        </tr>
    </table>*@




    
 
}
</body>
</html>
